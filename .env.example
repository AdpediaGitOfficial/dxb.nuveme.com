# Absolute origin of the deployed site. Used for canonical URLs, sitemap,
# robots.txt and Open Graph image URLs — set it in every environment.
NEXT_PUBLIC_SITE_URL=https://www.nuveproperties.com

# Search Console / Bing verification tokens (optional).
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=
